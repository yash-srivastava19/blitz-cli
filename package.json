{
  "name": "blitz-cli",
  "version": "0.1.0",
  "description": "Zero-friction timeboxing CLI — start a task, everything else happens via keypresses",
  "bin": {
    "blitz": "./src/cli.ts"
  },
  "type": "module",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun run build.ts"
  },
  "dependencies": {
    "@types/react": "^18.3.0",
    "ink": "^5.1.0",
    "ink-text-input": "^6.0.0",
    "react": "^18.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
